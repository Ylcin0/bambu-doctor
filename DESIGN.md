# 设计文档

> 这个项目要做什么、为谁做、不做什么。**改方向先改这份文档。**

---

## 一句话

**Bambu Lab 用户的打印问题顾问**：拍张照片，它告诉你哪里出了问题、该改哪个参数。

## 为谁

**使用 Bambu Studio 切片、遇到打印质量问题时靠搜论坛和试错的普通用户。**

- 不是给工程师的（他们自己会看参数）
- 不是给完全不懂的（他们需要的是陪伴式问答，不是工具）
- **作者自己的 A1 只是测试机**，不是主要服务对象

## 解决什么

两个场景，共用一个知识底座：

### A. 打印前 —— "这样配会怎样"

给定「模型 + 耗材 + 工艺档」，检查参数组合是否有冲突、有没有需要注意的地方。
理想形态：读入 3MF → 分析模型特征 + 参数 → 输出改好的 3MF，拖回 Studio 就能打。

### B. 打坏了 —— "为什么这样"

给定症状（文字描述或**照片**），结合用户的实际参数，给出：

1. 可能是什么问题（按可能性排序）
2. **该改哪个参数、改成多少**
3. 判断依据（你的哪个参数、什么值、对比什么基线）
4. 预期效果和代价
5. 哪些解释**不成立**（省得用户瞎试）

---

## 核心机制（最关键的设计决定）

```
照片 ──[AI 视觉]──→ 症状 ID（结构化标签）
                        ↓
               [知识库] 症状 → 候选根因（多个，带权重）
                        ↓
     [用户的 profile 真值 + 官方基线] 逐个验证条件是否成立
                        ↓
        输出三组：判断 / 次要 / 已排除 + 具体动作
```

### 分工原则

| 谁 | 负责 | 为什么 |
|---|---|---|
| **AI** | **感知**：看图识别症状、把结论说成人话 | 视觉识别不可替代 |
| **知识库** | **推理**：症状 → 哪些参数可能有问题 | 确定性逻辑，可写测试、可积累经验 |
| **用户参数** | **个性化**：把通用根因变成具体数字 | 这是"有用建议"和"通用废话"的分界线 |

### 硬约束

> **绝不让 AI 独自给建议。**

照片进、建议出的一把梭做法有三个致命问题：结论不可测（写不出测试）、不可积累（换个模型结论就变）、会自信地胡说（视觉模型最常见的失败模式）。

AI 只负责"这是什么现象"，"该改什么"由知识库和用户的实际参数决定。这样**换 AI 服务商也不影响结论正确性**，知识库还能一条条验证、一条条积累。

另外两条：

- **"不知道"是一等公民** —— 症状识别不出来就说不知道，不硬凑答案
- **规则要标适用条件**（机型/料/环境）—— 不满足就明说"这条不适用于你的配置"，而不是默默套用

---

## 不做什么

| 不做 | 原因 |
|---|---|
| GUI / 桌面应用 | CLI 先行验证；界面后补 |
| 云端服务、账号体系 | 纯本地是卖点（隐私 + 离线可用） |
| 切片功能 | 不重新发明 Bambu Studio |
| Bambu Studio 插件 | **已查实：Studio 没有给第三方加界面的插件系统**，它唯一的插件类型是网络通信插件（C ABI，约 100 个导出函数），不提供 UI 扩展点 |
| 内置 Bambu 官方预设数据 | 版权归 Bambu Lab，只从用户本机读 |
| 面向新手的陪伴式问答 | 那是对话助手的活，不是命令行工具的 |
| 承诺"100% 准确" | 给判断依据，不替用户拍板 |

---

## 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.9+ | 复用已有代码；零第三方依赖 |
| AI 接口 | **OpenAI 兼容**（默认 DeepSeek） | 换服务商只改配置，别人能填自己的 key |
| 图片格式 | `image_url` + `data:` URL（base64） | 实测可用 |
| 关闭思考 | `reasoning_effort: "none"` | OpenAI 标准字段，实测有效且通用 |
| 配置格式 | TOML | 人类可读，与已有的规则豁免配置一致 |
| 知识库 | 数据文件（TOML/YAML），不是硬编码 | **便于他人贡献**：发现规则不对能提 PR |

### 已实测踩过的坑（实现时必须处理）

1. **`max_tokens` 给少了会静默返回空字符串** —— 思考 token 吃光额度，正文一个字没有，而 API 返回 200。必须检查正文非空，空则重试或明确报错。
2. **看图任务思考 token 消耗大**（实测简单问题 34、看截图 135），所以默认关思考不只是省钱，也是避免"钱花了拿不到正文"。
3. **`enable_thinking: false` 无效**（DeepSeek 风格），要用 `reasoning_effort: "none"`。实测对照：`thinking.type=disabled` 和 `reasoning_effort=none` 有效，其余三种写法无效。

---

## 与现有代码的关系

项目从「profile 体检工具」扩展而来，已有的部分**全部复用**：

| 已有 | 在新定位里的角色 |
|---|---|
| profile 索引 + `inherits` 继承链解析 | 提供**参数真值**（诊断的基础） |
| 机型识别（A1 ≠ A1 mini ≠ A1M） | 选择正确的官方基线 |
| 官方基线对照 | 判断"偏离"的依据 |
| 现有 6 条体检规则 | R2/R3/R4 直接就是候选根因（跨机型继承 / 温度偏离 / 参数落空） |
| 55 个测试 + CI | 继续守护不回归 |

新增的部分：**症状库 + 根因库 + 推理引擎 + AI 接入**。

---

## 里程碑

| | 内容 | 验收标准 |
|---|---|---|
| **M1** | 症状库结构 + 规则引擎 + `diagnose` 命令（**文字描述症状，先不接 AI**） | 用作者真实遇到的案例跑，结论与实战经验一致 |
| **M2** | 接 AI 视觉：照片 → 症状 ID | 拍一张真实打印缺陷照片，能落到正确的症状上 |
| **M3** | 3MF 导入 + 参数优化输出 | 读 3MF → 改参数 → 输出能拖回 Studio 打 |
| **M4** | 反馈闭环：记录"按建议改完结果如何" | 能统计出哪些规则真的有效 |

**M1 先不接 AI 是刻意的**：知识库结构是整个项目风险最高的部分（结构错了，AI 接得再好也没用）。先用文字输入把结构跑通、把规则写出来、把测试写上，AI 只是之后加的一种输入方式。

---

## 成功标准

- [ ] 用它诊断作者自己的打印问题，结论和实战经验一致
- [ ] 陌生人 clone 下来、不改一行代码，能诊断自己的问题
- [ ] 每条建议都能追溯到具体的参数值（不是"试试看"）
- [ ] 每条规则都有测试，且至少包含一个"该成立"和一个"该排除"的用例
- [ ] 别人能通过提 PR 往知识库里加规则

## 关于 AI 的透明性（面向陌生人的责任）

工具会调用第三方 AI 服务，必须让用户清楚：

- **照片会上传到 AI 服务商**（要明说，且提供不用 AI 的纯规则模式）
- **每次调用有成本**（要说明大概多少钱、key 怎么配、能不能用本地模型）
- **API key 只存在本地配置里**，不上传、不进仓库
